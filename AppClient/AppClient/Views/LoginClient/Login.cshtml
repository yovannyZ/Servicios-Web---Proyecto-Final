@model AppClient.CanchitaWS.Usuario

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
}


@using (Html.BeginForm("Login", "LoginClient", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
     <div id="login-page" class="row">
    <div class="col s12 z-depth-4 card-panel">
      <form class="login-form">
        <div class="row">
          <div class="input-field col s12 center">
              <img src="~/img/anyone.png" alt="" class="circle responsive-img valign profile-image-login">
            <p class="center login-form-text" >identificate para poder hacer una reserva</p>
          </div>
        </div>
        <div class="row margin">
          <div class="input-field col s12">
              <i class="material-icons prefix">perm_identity</i>
              @Html.TextBoxFor(model => model.Username, new { @required = "required" })
            <label for="Username" class="center-align">Username</label>
          </div>
        </div>
        <div class="row margin">
          <div class="input-field col s12">
              <i class="material-icons prefix">lock_outline</i>
              @Html.PasswordFor(model => model.Clave, new { @required = "required" })
            <label for="Clave">Password</label>
          </div>
        </div>
        <div class="row">      
        </div>
        <div class="row">
          <div class="input-field col s12">
              <button class="btn waves-effect waves-light col s12" type="submit" name="action">Entrar</button>
              <p class="red-text text-darken-3 center-align">@ViewBag.Error</p>
          </div>
        </div>
        <div class="row">
          <div class="input-field col s6 m6 l6">
            <p class="margin medium-small"><a href="@Url.Action("Registro", "LoginClient")">Registrate ahora!</a></p>
          </div>
          <div class="input-field col s6 m6 l6">
              <p class="margin right-align medium-small"><a href="@Url.Action("RecuperarAccXEmail", "LoginClient")">¿Olvidaste tu usuario y/o contraseña?</a></p>
          </div>          
        </div>

      </form>
    </div>
  </div>    
}

